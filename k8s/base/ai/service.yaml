apiVersion: v1
kind: Service
metadata:
  name: hammerly-ai
  namespace: hammerly
  labels:
    app.kubernetes.io/name: hammerly-ai
    app.kubernetes.io/component: ai
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: hammerly-ai
  ports:
    - name: http
      port: 5001
      targetPort: http

